
    $("#questions").html("<%= j render @questions %>");
    $("#pag").html("<%= j will_paginate @questions, class: 'col-md-12' %>");
    $(".number-questions").html("( <%= pluralize(@questions.count, 'Question') %> )");
