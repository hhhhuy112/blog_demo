<tr>
  <td><%= category.id %></td>
  <td><%= link_to category.name, category %></td>
  <td>
    <%= link_to t("edit"), category, class: "btn btn-primary" %>
    <%= link_to t("delete"), category, method: :delete, data: { confirm: t("are_you_sure"), remote: true }, class: "btn btn-danger" %>
  </td>
</tr>
